@model List<Statistic>

<div class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="card ">
                <div class="card-header">
                    <h4 class="card-title"> Statistics Table</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table tablesorter " id="">
                            <thead class=" text-primary">
                                <tr>
                                    <th>
                                        Key
                                    </th>
                                    <th>
                                        Value
                                    </th>
                                </tr>
                            </thead>
                            @if (Model.Count() != 0)
                            {
                                <tbody>
                                    @foreach (Statistic stat in Model)
                                    {
                                        <tr>
                                            <td>
                                                @stat.Title
                                            </td>
                                            <td>
                                                @stat.Desc
                                            </td>
                                            <td class="text-center" style="cursor:pointer">
                                                <a asp-area="admin" asp-controller="statistic" asp-action="delete" asp-route-id="@stat.Id"> <i class="fa-regular fa-trash-can"></i></a>
                                            </td>
                                            <td class="text-center" style="cursor:pointer">
                                                <a asp-area="admin" asp-controller="statistic" asp-action="update" asp-route-id="@stat.Id"><i class="fa-solid fa-pen"></i></a>
                                            </td>
                                        </tr>

                                    }
                                    <tr>
                                        <td><a asp-area="admin" asp-controller="statistic" asp-action="create" class="btn btn-info">Create </a> </td>
                                    </tr>
                                </tbody>
                            }
                            else
                            {
                                <tbody>
                                    <tr class="text-info"> <td>No Statistics Yet</td> </tr>
                                    <tr>
                                        <td><a asp-area="admin" asp-controller="statistic" asp-action="create" class="btn btn-info">Create </a> </td>
                                    </tr>

                                </tbody>
                            }

                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>